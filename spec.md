# 仕様書: 動物カフェ検索アプリ「いまから、もふもふ」

> このファイルはAIエージェント向けの実装仕様書です。
> 仕様を確認し、実装後に動作確認まで行ってください。

---

## 🎯 目的

現在時刻と利用者の希望条件（ふれあい重視 / 安さ重視 / 近さ重視）をもとに、いま営業中の動物カフェを重み付けスコアで採点し、おすすめ順に並べて比較できるようにするWebアプリ。猫・サモエド・ハリネズミ・ブタなど複数の動物カフェが候補にあるとき、「自分が一番行きたい店はどれか」を数値で判断できるようにすることが狙い。

---

## 🛠 技術スタック

| 項目 | 内容 |
|------|------|
| 言語 | Python 3.11+ |
| 実行環境 | ローカルPC（Windows想定）。将来的に AWS（RDS + App Runner 等）へ公開しスマホのブラウザから利用 |
| 主要ライブラリ | `streamlit`, `psycopg2-binary`, `pandas` |
| データベース | PostgreSQL 15+ |
| 外部API | なし（店データは手入力CSVから投入） |

---

## 📥 入力

### 1. 店舗データ（初期投入・1回だけ）

`data/shops.csv` を手入力で用意し、スクリプトで PostgreSQL に投入する。20〜30件を想定。

```csv
name,animal,area,walk_min,price_30min,contact_level,animal_count,quietness,open_time,close_time
猫カフェ にゃんこ,猫,横浜,5,800,4,15,3,11:00,20:00
サモエドカフェ もふもふ,サモエド,川崎,8,1500,5,6,2,12:00,19:00
ハリネズミの森,ハリネズミ,桜木町,3,1200,3,20,5,10:00,18:00
```

- `contact_level`（ふれあい度）, `quietness`（静かさ）は 1〜5 の5段階
- `walk_min` は最寄駅からの徒歩分
- `price_30min` は30分あたりの料金（円）

### 2. 実行時のユーザー入力（画面上）

- ふれあい重視の重み: 0〜10 のスライダー（初期値 5）
- 安さ重視の重み: 0〜10 のスライダー（初期値 5）
- 近さ重視の重み: 0〜10 のスライダー（初期値 5）
- 動物のしゅるい: セレクトボックス（「ぜんぶ」＋DBに存在する動物の一覧）
- 「いま営業中の店のみ表示」チェックボックス（初期値 ON）

```
起動コマンド:
streamlit run app.py

CLI版（先に作る）:
python main.py
```

---

## 📤 出力 / 完了条件

- ブラウザに、条件に合う店舗がスコアの高い順にカード形式で表示される
- 各カードに「順位」「動物の絵文字＋店名」「エリア・徒歩分・料金」「スコア」「スコアを表すバー」が表示される
- 1位のカードだけ枠線を強調する
- スライダーを動かすと即座に順位が入れ替わる
- 該当0件のときは「いま行けるお店が見つかりませんでした」と表示する

```
CLI版の出力例:
1位  🐶 サモエドカフェ もふもふ  9.2点  (川崎 / 徒歩8分 / 1500円)
2位  🐱 猫カフェ にゃんこ        6.3点  (横浜 / 徒歩5分 / 800円)
3位  🦔 ハリネズミの森          3.9点  (桜木町 / 徒歩3分 / 1200円)
```

---

## ⚙️ 処理フロー

1. PostgreSQL に接続し、`shops` テーブルから全店舗を取得する
2. 動物の種類が指定されていれば、その種類だけに絞り込む
3. 「いま営業中の店のみ」が ON なら、現在時刻が `open_time` 〜 `close_time` に含まれる店だけに絞り込む
4. 絞り込み後の店舗集合に対して、評価3項目をそれぞれ 0〜1 に正規化する（min-max正規化）
   - ふれあい度: 値が大きいほど良い → `(x - min) / (max - min)`
   - 安さ: 値が小さいほど良い → `(max - x) / (max - min)`
   - 近さ: 値が小さいほど良い → `(max - x) / (max - min)`
   - `max == min` のときは 0除算を避けるため、全店に 1.0 を与える
5. スコアを計算する
   `score = w_contact × n_contact + w_price × n_price + w_walk × n_walk`
6. スコアの降順に並べ替え、上位から画面に表示する

### 計算例（検証用）

上記CSVの3店、重みが「ふれあい5 / 安さ2 / 近さ3」のとき:

| 店名 | ふれあい | 安さ | 近さ | スコア |
|---|---|---|---|---|
| 猫カフェ にゃんこ | 0.5 | 1.0 | 0.6 | 6.30 |
| サモエドカフェ もふもふ | 1.0 | 0.0 | 0.0 | 5.00 |
| ハリネズミの森 | 0.0 | 0.43 | 1.0 | 3.86 |

重みを「ふれあい10 / 安さ1 / 近さ1」に変えると、サモエドカフェが 10.00 で1位に入れ替わること。

---

## 🗄 データベース定義

```sql
CREATE TABLE shops (
    id            SERIAL PRIMARY KEY,
    name          VARCHAR(100) NOT NULL,
    animal        VARCHAR(50)  NOT NULL,
    area          VARCHAR(50)  NOT NULL,
    walk_min      INTEGER      NOT NULL,
    price_30min   INTEGER      NOT NULL,
    contact_level INTEGER      NOT NULL CHECK (contact_level BETWEEN 1 AND 5),
    animal_count  INTEGER      NOT NULL,
    quietness     INTEGER      NOT NULL CHECK (quietness BETWEEN 1 AND 5),
    open_time     TIME         NOT NULL,
    close_time    TIME         NOT NULL
);
```

---

## 📁 ファイル構成

```
animal-cafe-app/
├── .streamlit/
│   └── config.toml       # 画面テーマ
├── data/
│   └── shops.csv         # 手入力の店舗データ
├── db.py                 # DB接続とSELECT
├── scoring.py            # 正規化とスコア計算（画面に依存しない純粋な関数）
├── main.py               # CLI版のエントリポイント
├── app.py                # Streamlit版のエントリポイント
├── import_csv.py         # CSVをPostgreSQLに投入するスクリプト
└── requirements.txt
```

`scoring.py` は Streamlit にも DB にも依存させないこと。引数として店舗のリストと重みを受け取り、スコア付きのリストを返す純粋な関数として実装する。CLI版と画面版の両方から同じ関数を呼べる状態にすること。

### 画面テーマ（`.streamlit/config.toml`）

```toml
[theme]
backgroundColor = "#FDF8F3"
secondaryBackgroundColor = "#FBEDF0"
primaryColor = "#E8A0B4"
textColor = "#5C4A42"
font = "sans serif"
```

### 動物の絵文字対応

```python
ANIMAL_EMOJI = {
    "猫": "🐱",
    "サモエド": "🐶",
    "ハリネズミ": "🦔",
    "ブタ": "🐷",
}
emoji = ANIMAL_EMOJI.get(shop["animal"], "🐾")
```

---

## ⚠️ エラー処理

- DB接続に失敗した場合: 例外を捕捉し、「データベースに接続できませんでした」と画面に表示してプログラムを止める。スタックトレースをそのまま利用者に見せない
- 条件に合う店が0件の場合: エラーにせず、「いま行けるお店が見つかりませんでした」と案内し、条件を緩める提案を表示する
- 正規化で `max == min` になる場合（該当店が1件のみ、または全店同値）: 0除算を避け、全店に 1.0 を与える
- 重みが全て0の場合: 全店のスコアが0になるため、「重みを1つ以上設定してください」と案内する
- `ANIMAL_EMOJI` に存在しない動物種: `.get()` の既定値 `🐾` を使い、例外を発生させない
- CSVに欠損値や不正な値がある場合: 投入時にその行をスキップし、何行目をスキップしたかを標準出力に表示する

---

## ✅ 動作確認チェックリスト

AIエージェントは実装後、以下をすべて確認してください。

- [ ] `import_csv.py` を実行すると、`shops` テーブルにCSVの全行が投入されること
- [ ] `python main.py` がCLIで動作し、スコア順の一覧が表示されること
- [ ] `streamlit run app.py` でブラウザが開き、スライダーとカード一覧が表示されること
- [ ] 「計算例（検証用）」の3店・重み(5,2,3)で、スコアが 6.30 / 5.00 / 3.86 になること
- [ ] 重みを (10,1,1) に変えるとサモエドカフェが1位になること
- [ ] 「いま営業中の店のみ」を ON にすると、現在時刻に閉まっている店が消えること
- [ ] 該当0件のときにエラーで落ちず、案内メッセージが出ること
- [ ] 重みを全て0にしてもクラッシュしないこと
- [ ] DBを停止した状態で起動しても、エラーメッセージが表示されて落ちるだけで済むこと
- [ ] `config.toml` のテーマが反映され、背景が生成り色・スライダーがピンクになっていること

---

## 📝 備考・制約

### 実装レベルの制約（重要）

このアプリは職業訓練校の制作演習であり、作者本人が制作発表でコードを説明する必要がある。以下を守ること。

- 関数を中心に書く。クラスは使わない（必要なら理由をコメントで書く）
- デコレータは `@st.cache_data` 程度にとどめ、自作しない
- 内包表記は1段まで。ネストした内包表記や `lambda` の多用を避ける
- 型ヒントは付けてよいが、`typing` の高度な機能（`Protocol`, `TypeVar` 等）は使わない
- 変数名・関数名は英語、コメントは日本語で書く
- 「なぜそう書いたか」が分かるコメントを、各関数の先頭に1〜3行入れる
- SQLは文字列連結ではなくプレースホルダを使う（SQLインジェクション対策として説明できるようにするため）

### 実装の進め方

1. `scoring.py` と `main.py`（CLI版）を先に完成させ、計算が正しいことを確認する
2. そのあとで `app.py`（Streamlit版）を作り、`scoring.py` の関数を呼び出す
3. 画面とロジックを分離した理由を説明できる状態にしておく

一度に全機能を実装せず、上記の順に区切って進め、各段階で動作確認の結果を報告すること。

### 将来的な拡張（今回は実装しない）

- AWS（RDS + App Runner）へのデプロイ
- 上位3件のレーダーチャート比較
- 現在地の緯度経度からの距離計算

---

*この仕様書は [Playground Spec スキル] によって生成されました。*
